import { DossierCountry } from "@/components/modules/dossier-country";

/** Сводка по стране: субъекты, признаки риска, связи и активы по всей базе. */
export default function DossierOverviewPage() {
  return <DossierCountry />;
}
