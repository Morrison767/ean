import { EsfCountry } from "@/components/modules/esf-country";

/** Сводка по стране: обороты, контрагенты, товары и риски по всему реестру. */
export default function EsfOverviewPage() {
  return <EsfCountry />;
}
